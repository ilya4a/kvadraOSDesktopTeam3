syntax = "proto3";

service AccelerometerService {
rpc StreamAccelData(stream AccelPacket) returns (stream AccelModule);
}

message AccelPacket {
int64 timestamp = 1;
float x = 2;
float y = 3;
float z = 4;
}

message AccelModule {
int64 timestamp = 1;
float module = 2;
}